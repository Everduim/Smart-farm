import React from 'react';


const Nav = ({ isOpen }) => {
  return (
    <nav className={`nav ${isOpen ? 'nav--open' : ''}`}>
      <link rel="stylesheet" href="fegege" />
    </nav>
  );
};

export default Nav;
